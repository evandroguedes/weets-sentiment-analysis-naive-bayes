# Tweets Sentiment Analysis - Naive Bayes studies

Sentiment Analysis (NLP) studies on Python.

## Installation

Use the package manager [pip](https://pip.pypa.io/en/stable/) to install dependencies.

```bash
pip install -r requirements.txt
```
